# 🚀 Установка WireGuard VPN на Ubuntu 24.04 VPS

## 📋 Требования
- VPS с Ubuntu 24.04
- Root доступ или sudo
- Открытый порт 51820 (UDP)
- Ваш сайт уже работает на VPS (мы не будем его трогать!)

## ⚡ Быстрая установка (автоматический скрипт)

### Шаг 1: Подключитесь к VPS через MobaXterm

```bash
ssh root@ваш_ip_адрес
```

### Шаг 2: Скачайте и запустите скрипт установки

```bash
wget https://git.io/wireguard -O wireguard-install.sh && bash wireguard-install.sh
```

**Скрипт спросит:**
1. **Какой IPv4 адрес использовать?** - Нажмите Enter (автоматически)
2. **Какой порт использовать?** - Введите `51820` или нажмите Enter
3. **Какой DNS использовать?** - Выберите `1` (Cloudflare - лучше для игр!)
4. **Имя первого клиента?** - Введите `client1` или любое имя

### Шаг 3: Скрипт создаст конфигурацию

После установки вы увидите QR-код и путь к файлу конфигурации:
```
Client configuration:
/root/client1.conf
```

### Шаг 4: Получите конфигурацию

```bash
cat /root/client1.conf
```

Скопируйте всё содержимое - это ваша конфигурация!

---

## 🔧 Ручная установка (если автоскрипт не подошёл)

### 1. Обновите систему

```bash
apt update && apt upgrade -y
```

### 2. Установите WireGuard

```bash
apt install wireguard -y
```

### 3. Создайте ключи сервера

```bash
cd /etc/wireguard
umask 077
wg genkey | tee server_private.key | wg pubkey > server_public.key
```

### 4. Создайте ключи клиента

```bash
wg genkey | tee client_private.key | wg pubkey > client_public.key
wg genpsk > preshared.key
```

### 5. Создайте конфигурацию сервера

```bash
nano /etc/wireguard/wg0.conf
```

Вставьте (замените ВАШИ_КЛЮЧИ):

```ini
[Interface]
Address = 10.84.34.1/24
ListenPort = 51820
PrivateKey = СОДЕРЖИМОЕ_server_private.key
PostUp = iptables -A FORWARD -i %i -j ACCEPT; iptables -A FORWARD -o %i -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
PostDown = iptables -D FORWARD -i %i -j ACCEPT; iptables -D FORWARD -o %i -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE

[Peer]
PublicKey = СОДЕРЖИМОЕ_client_public.key
PresharedKey = СОДЕРЖИМОЕ_preshared.key
AllowedIPs = 10.84.34.2/32
```

**Важно:** Замените `eth0` на имя вашего сетевого интерфейса (узнать: `ip a`)

### 6. Включите IP forwarding

```bash
echo "net.ipv4.ip_forward = 1" >> /etc/sysctl.conf
echo "net.ipv6.conf.all.forwarding = 1" >> /etc/sysctl.conf
sysctl -p
```

### 7. Настройте firewall

```bash
# Открываем порт WireGuard
ufw allow 51820/udp

# Разрешаем форвардинг
ufw route allow in on wg0 out on eth0

# Включаем UFW (если ещё не включен)
ufw enable
```

### 8. Запустите WireGuard

```bash
systemctl enable wg-quick@wg0
systemctl start wg-quick@wg0
systemctl status wg-quick@wg0
```

### 9. Создайте конфигурацию для клиента

```bash
nano /root/client1.conf
```

Вставьте (замените ВАШИ_КЛЮЧИ и IP_СЕРВЕРА):

```ini
[Interface]
PrivateKey = СОДЕРЖИМОЕ_client_private.key
Address = 10.84.34.2/24
DNS = 1.1.1.1, 1.0.0.1
MTU = 1420

[Peer]
PublicKey = СОДЕРЖИМОЕ_server_public.key
PresharedKey = СОДЕРЖИМОЕ_preshared.key
Endpoint = IP_ВАШЕГО_VPS:51820
AllowedIPs = 0.0.0.0/0, ::0/0
PersistentKeepalive = 25
```

---

## 🎮 Оптимизация для игр

### 1. Уменьшите задержку с помощью BBR

```bash
echo "net.core.default_qdisc=fq" >> /etc/sysctl.conf
echo "net.ipv4.tcp_congestion_control=bbr" >> /etc/sysctl.conf
sysctl -p
```

### 2. Оптимизируйте сетевые буферы

```bash
cat >> /etc/sysctl.conf << EOF
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
net.ipv4.tcp_rmem = 4096 87380 67108864
net.ipv4.tcp_wmem = 4096 65536 67108864
net.ipv4.tcp_mtu_probing = 1
EOF

sysctl -p
```

### 3. Проверка ping и скорости

```bash
# Проверка статуса
wg show

# Проверка подключенных клиентов
wg show wg0 peers
```

---

## 📱 Добавление новых клиентов

Если хотите добавить ещё клиентов (друзей), используйте скрипт:

```bash
bash wireguard-install.sh
```

Выберите опцию "Add a new client" и введите имя.

---

## 🔥 Мульти-серверная конфигурация

Хотите несколько серверов? Просто:

1. Повторите установку на другом VPS
2. В клиентском приложении создайте несколько профилей
3. Переключайтесь между серверами в один клик!

### Пример: США сервер

Для второго сервера используйте другой порт (например, 51821) или другой VPS.

---

## 🛡️ Безопасность

### Настройка fail2ban (защита от брутфорса SSH)

```bash
apt install fail2ban -y
systemctl enable fail2ban
systemctl start fail2ban
```

### Смена SSH порта

```bash
nano /etc/ssh/sshd_config
# Измените: Port 22 -> Port 2222
systemctl restart sshd
```

Не забудьте открыть новый порт: `ufw allow 2222/tcp`

---

## 🧪 Тестирование VPN

### На сервере:

```bash
# Проверка запущен ли WireGuard
systemctl status wg-quick@wg0

# Проверка подключений
wg show

# Логи
journalctl -u wg-quick@wg0 -f
```

### На клиенте (Windows):

1. Откройте Freedom VPN
2. Нажмите Connect
3. Проверьте IP: https://whatismyipaddress.com
4. Проверьте ping: `ping 8.8.8.8`
5. Протестируйте в игре!

---

## ❓ Решение проблем

### Проблема: "Не могу подключиться"

```bash
# Проверьте статус
systemctl status wg-quick@wg0

# Перезапустите
systemctl restart wg-quick@wg0

# Проверьте открыт ли порт
netstat -ulnp | grep 51820

# Проверьте firewall
ufw status
```

### Проблема: "Подключился, но нет интернета"

```bash
# Проверьте IP forwarding
sysctl net.ipv4.ip_forward
# Должно быть: net.ipv4.ip_forward = 1

# Проверьте NAT правила
iptables -t nat -L -n -v
```

### Проблема: "Высокий ping в игре"

1. Уменьшите MTU в конфиге клиента: `MTU = 1380` или `1360`
2. Используйте сервер ближе к вам географически
3. Включите BBR (см. оптимизацию выше)

### Проблема: "Отключается через несколько минут"

- Проверьте что `PersistentKeepalive = 25` в конфиге клиента
- Увеличьте значение до 15 секунд: `PersistentKeepalive = 15`

---

## 📊 Мониторинг использования

```bash
# Установка vnstat
apt install vnstat -y

# Мониторинг трафика
vnstat -i wg0
```

---

## 🔄 Обновление конфигурации

После изменения `/etc/wireguard/wg0.conf`:

```bash
systemctl restart wg-quick@wg0
```

---

## 🎯 Важные команды

```bash
# Запуск
wg-quick up wg0

# Остановка
wg-quick down wg0

# Статус
wg show

# Просмотр конфигурации
cat /etc/wireguard/wg0.conf

# Просмотр логов
journalctl -u wg-quick@wg0 --since today
```

---

## 💡 Советы

1. **Для игр** используйте `PersistentKeepalive = 25` - предотвращает таймауты NAT
2. **Для скорости** используйте `MTU = 1420` - предотвращает фрагментацию пакетов
3. **Для пинга** используйте Cloudflare DNS (1.1.1.1) - самый быстрый
4. **Для безопасности** меняйте SSH порт и используйте fail2ban
5. **Для стабильности** включите BBR congestion control

---

## 🌐 Проверка вашего IP после подключения

После подключения откройте в браузере:
- https://whatismyipaddress.com
- https://ipleak.net

Должен отображаться IP вашего VPS!

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `journalctl -u wg-quick@wg0 -f`
2. Проверьте открыт ли порт: `netstat -ulnp | grep 51820`
3. Проверьте firewall: `ufw status verbose`
4. Проверьте подключение: `wg show`

---

## ✅ Готово!

Теперь у вас работает собственный VPN сервер! 🎉

Вы можете:
- ✨ Безопасно играть в игры
- 🌍 Обходить блокировки
- 🔒 Защитить свой трафик
- 🚀 Делиться VPN с друзьями

**Следующий шаг:** Скопируйте конфигурацию из `/root/client1.conf` и вставьте в Freedom VPN приложение!
